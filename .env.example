# XPremChatbot environment variables
# All AI is routed through puter.js — no API keys required for local dev.
# These flags only tune runtime behavior.

# Default model id (any puter.js-supported model)
VITE_DEFAULT_MODEL=gpt-5-nano

# Show developer/diagnostic panel
VITE_SHOW_DIAGNOSTICS=false
